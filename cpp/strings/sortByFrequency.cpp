#include <bits/stdc++.h>
using namespace std;

string sortByFrequency(string s) {
  int count = 1;
  string ans;

  int preMax = 1;

  for (int i = 0; i < s.size() - 1; i++) {
  }
  return ans;
}

int main() {

  string s = "abbdccc";
  cout << sortByFrequency(s) << endl;

  return 0;
}
